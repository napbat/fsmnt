repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: check-toml
      - id: check-yaml
      - id: end-of-file-fixer
      - id: mixed-line-ending
      - id: trailing-whitespace

  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: cargo fmt --all -- --check
        language: system
        types: [rust]
        pass_filenames: false
      - id: cargo-clippy
        name: cargo clippy (pedantic, deny warnings)
        entry: cargo clippy --workspace --all-targets --all-features -- -D warnings
        language: system
        types: [rust]
        pass_filenames: false
      - id: max-file-lines
        name: rust files must not exceed 1000 lines
        entry: python scripts/check_max_lines.py
        language: python
        types: [rust]
      - id: formats-no-std
        name: filesystem format crates stay no_std
        entry: python scripts/check_formats_no_std.py
        language: python
        files: ^(Cargo\.toml|Cargo\.lock|crates/formats/.+\.(rs|toml)|scripts/check_formats_no_std\.py)$
        pass_filenames: false
