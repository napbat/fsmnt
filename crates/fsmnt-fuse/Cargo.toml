[package]
name = "fsmnt-fuse"
version.workspace = true
edition.workspace = true

# All dependencies are Unix-only: on other targets this crate compiles to
# an empty library so workspace-wide builds work everywhere.
[target.'cfg(unix)'.dependencies]
chrono = { workspace = true }
ctrlc = { workspace = true }
fsmnt-core = { workspace = true }
fuser = { workspace = true }
libc = { workspace = true }

[lints]
workspace = true
