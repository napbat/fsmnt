[package]
name = "fsmnt-device-linux"
version.workspace = true
edition.workspace = true

# All dependencies are Linux-only: on other targets this crate compiles to
# an empty library so workspace-wide builds work everywhere.
[target.'cfg(target_os = "linux")'.dependencies]
fsmnt-device = { workspace = true }
fsmnt-proxy = { workspace = true }
libc = { workspace = true }

[lints]
workspace = true
