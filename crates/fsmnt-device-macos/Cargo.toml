[package]
name = "fsmnt-device-macos"
version.workspace = true
edition.workspace = true

# All dependencies are macOS-only: on other targets this crate compiles to
# an empty library so workspace-wide builds work everywhere.
[target.'cfg(target_os = "macos")'.dependencies]
fsmnt-device = { workspace = true }
fsmnt-proxy = { workspace = true }
libc = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true
